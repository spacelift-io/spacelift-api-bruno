meta {
  name: Delete Version
  type: graphql
  seq: 5
}

docs {
  ⚠ **DEPRECATED** — Use templateVersionDelete instead
}

post {
  url: {{SPACELIFT_ENDPOINT}}
  body: graphql
  auth: inherit
}

body:graphql {
  mutation DeleteBlueprintVersion($id: ID!) {
    blueprintVersionDelete(id: $id) {
      id
      name
    }
  }
}

body:graphql:vars {
  {
    "id": "VERSION_ID_HERE"
  }
}
