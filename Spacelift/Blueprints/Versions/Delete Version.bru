meta {
  name: Delete Version
  type: graphql
  seq: 5
}

post {
  url: {{SPACELIFT_ENDPOINT}}
  body: graphql
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:graphql {
  mutation DeleteBlueprintVersion($id: ID!) {
    blueprintVersionDelete(id: $id) {
      id
      name
    }
  }
}

body:graphql:vars {
  {
    "id": "VERSION_ID_HERE"
  }
}
