meta {
  name: Delete Stack Role Binding
  type: graphql
  seq: 5
}

post {
  url: {{SPACELIFT_ENDPOINT}}
  body: graphql
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:graphql {
  mutation DeleteStackRoleBinding($id: ID!) {
    stackRoleBindingDelete(id: $id) {
      id
      stackID
    }
  }
}

body:graphql:vars {
  {
    "id": "ROLE_BINDING_ID_HERE"
  }
}
