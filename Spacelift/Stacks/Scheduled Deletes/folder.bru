meta {
  name: Scheduled Deletes
  seq: 23
}

docs {
  Schedules a stack, and optionally the resources it manages, to be destroyed at a
  set time. The usual reason is an ephemeral environment that should clean itself
  up.
}
