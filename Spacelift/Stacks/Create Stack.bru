meta {
  name: Create Stack
  type: graphql
  seq: 3
}

docs {
  Create a new stack. The third argument (stackObjectID) is optional, and
  should only be used when creating a Stack with pre-existing state that is
  handed over to Spacelift for further management.

  <!-- notes: hand-written, preserved by sync-docs -->

  The input is large and mostly optional. The quickest way to see the shape you
  need is to run **Get Stack** against a stack that already works and mirror it.

  `space` is required, and decides who can see the stack.
}

post {
  url: {{SPACELIFT_ENDPOINT}}
  body: graphql
  auth: inherit
}

body:graphql {
  mutation CreateStack($input: StackInput!, $manageState: Boolean!) {
    stackCreate(input: $input, manageState: $manageState) {
      id
      name
      description
      space
      repository
      branch
      projectRoot
      administrative
      autodeploy
      labels
    }
  }
}

body:graphql:vars {
  {
    "input": {
      "name": "my-stack",
      "description": "My new stack",
      "space": "root",
      "repository": "my-repo",
      "branch": "main",
      "projectRoot": "",
      "administrative": false,
      "autodeploy": false,
      "labels": []
    },
    "manageState": false
  }
}
