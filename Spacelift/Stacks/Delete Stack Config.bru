meta {
  name: Delete Stack Config
  type: graphql
  seq: 7
}

docs {
  Delete a stack config element.
}

post {
  url: {{SPACELIFT_ENDPOINT}}
  body: graphql
  auth: inherit
}

body:graphql {
  mutation DeleteStackConfig($stack: ID!, $id: ID!) {
    stackConfigDelete(stack: $stack, id: $id) {
      id
      type
      description
    }
  }
}

body:graphql:vars {
  {
    "stack": "{{?Stack ID}}",
    "id": "MY_ENV_VAR"
  }
}
