meta {
  name: GitLab
}

docs {
  Setup for GitLab, hosted or self-managed. **Get Webhooks Endpoint** returns the URL
  to register on the provider side, and **Regenerate Webhook Secret** rotates the
  shared secret.
}
