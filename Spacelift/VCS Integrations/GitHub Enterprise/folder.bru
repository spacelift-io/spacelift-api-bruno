meta {
  name: GitHub Enterprise
}

docs {
  Setup for self-hosted GitHub Enterprise. **Get Webhooks Endpoint** returns the URL
  to register on the provider side, and **Regenerate Webhook Secret** rotates the
  shared secret.
}
