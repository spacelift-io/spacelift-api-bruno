meta {
  name: Bitbucket Datacenter
}

docs {
  Setup for self-hosted Bitbucket Data Center. **Get Webhooks Endpoint** returns the
  URL to register on the provider side, and **Regenerate Webhook Secret** rotates
  the shared secret.
}
