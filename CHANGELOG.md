# Changelog

Every request added, corrected, deprecated or removed in this collection. For
changes to the Spacelift API itself, see the
[Spacelift changelog](https://docs.spacelift.io/product/changelog).

## 2026-09-15

- Removed **Runs → Delete Run State Summary** (`deleteRunStateSummary`)
- Removed **Runs → Delete Run Summary** (`deleteRunSummary`)
- Removed **Runs → Summarize Run** (`summarizeRun`)
- Removed **Runs → Summarize Run State** (`summarizeRunState`)
- Deprecated **Blueprints → Deployments → List Deployments** (`searchBlueprintDeployments`) — Use searchTemplateDeployments instead
- Deprecated **Blueprints → Deployments → Get Deployment** (`blueprintDeployment`) — Use templateDeployment instead
- Deprecated **Blueprints → Deployments → Create Deployment** (`blueprintDeploymentCreate`) — Use templateDeploymentCreate instead
- Deprecated **Blueprints → Deployments → Update Deployment** (`blueprintDeploymentUpdate`) — Use templateDeploymentUpdate instead
- Deprecated **Blueprints → Deployments → Delete Deployment** (`blueprintDeploymentDelete`) — Use templateDeploymentDelete instead
- Deprecated **Blueprints → Versioned Groups → List Versioned Groups** (`searchBlueprintVersionedGroups`) — Use searchTemplates instead
- Deprecated **Blueprints → Versioned Groups → Get Versioned Group** (`blueprintVersionedGroup`) — Use template instead
- Deprecated **Blueprints → Versioned Groups → Create Versioned Group** (`blueprintVersionedGroupCreate`) — Use templateCreate instead
- Deprecated **Blueprints → Versioned Groups → Update Versioned Group** (`blueprintVersionedGroupUpdate`) — Use templateUpdate instead
- Deprecated **Blueprints → Versioned Groups → Delete Versioned Group** (`blueprintVersionedGroupDelete`) — Use templateDelete instead
- Deprecated **Blueprints → Versions → Create Version** (`blueprintVersionCreate`) — Use templateVersionCreate instead
- Deprecated **Blueprints → Versions → Update Version** (`blueprintVersionUpdate`) — Use templateVersionUpdate instead
- Deprecated **Blueprints → Versions → Delete Version** (`blueprintVersionDelete`) — Use templateVersionDelete instead
- Deprecated **Blueprints → Versions → Parse Blueprint Version Template** (`blueprintVersionParseTemplate`) — Use the templateVersionParseTemplate query instead
- Deprecated **Modules → List Shared Modules** (`sharedModules`) — Use sharedModulesV2 field instead.
- Deprecated **Runs → Prioritize Run** (`runPrioritizeSet`) — Use runPrioritySet instead.
- Fixed **Ansible → List Ansible Hosts** (`ansibleHosts`) — its sample variables used values the API rejects
- Fixed **Ansible → List Ansible Tasks** (`ansibleTasks`) — its sample variables used values the API rejects
- Fixed **Audit Trail → Set Webhook Headers** (`auditTrailWebhookSetHeaders`) — its sample variables used values the API rejects
- Fixed **Audit Trail → Delete Webhook Headers** (`auditTrailWebhookDeleteHeaders`) — its sample variables used values the API rejects
- Fixed **Blueprints → Versioned Groups → Create Versioned Group** (`blueprintVersionedGroupCreate`) — its sample variables used values the API rejects
- Fixed **Blueprints → Versioned Groups → Update Versioned Group** (`blueprintVersionedGroupUpdate`) — its sample variables used values the API rejects
- Fixed **Blueprints → Versions → Create Version** (`blueprintVersionCreate`) — its sample variables used values the API rejects
- Fixed **Blueprints → Versions → Update Version** (`blueprintVersionUpdate`) — its sample variables used values the API rejects
- Fixed **Blueprints → Versions → Create Stack from Version** (`blueprintCreateStack`) — its sample variables used values the API rejects
- Fixed **Blueprints → Versions → Parse Blueprint Version Template** (`blueprintVersionParseTemplate`) — it appeared out of order in its folder
- Fixed **External Integrations → Create External Integration** (`externalIntegrationCreate`) — its sample variables used values the API rejects
- Fixed **Managed Users → User Groups → Create User Group** (`managedUserGroupCreateV2`) — its sample variables used values the API rejects
- Fixed **Managed Users → User Groups → Create User Group Integration** (`managedUserGroupIntegrationCreate`) — its sample variables used values the API rejects
- Fixed **Managed Users → User Groups → Update User Group Integration** (`managedUserGroupIntegrationUpdate`) — its sample variables used values the API rejects
- Fixed **Managed Users → Invite Managed User** (`managedUserInviteV2`) — its sample variables used values the API rejects
- Fixed **Managed Users → Update Managed User** (`managedUserUpdate`) — its sample variables used values the API rejects
- Fixed **Modules → Create Module** (`moduleCreate`) — its sample variables used values the API rejects
- Fixed **Modules → Share Module** (`moduleShare`) — its sample variables used values the API rejects
- Fixed **Modules → Update Module V2** (`moduleUpdateV2`) — its sample variables used values the API rejects
- Fixed **Plugins → Install Plugin** (`pluginInstall`) — its sample variables used values the API rejects
- Fixed **Plugins → Update Plugin** (`pluginUpdate`) — its sample variables used values the API rejects
- Fixed **Policies → Simulate Policy** (`policySimulatev2`) — its sample variables used values the API rejects
- Fixed **Repos → Create Revision** (`revisionCreate`) — its sample variables used values the API rejects
- Fixed **Roles → Stack Role Bindings → Update Stack Role Binding** (`stackRoleBindingUpdate`) — its sample variables used values the API rejects
- Fixed **Roles → Update Role** (`roleUpdate`) — its sample variables used values the API rejects
- Fixed **Runs → Set Run Priority** (`runPrioritySet`) — its sample variables used values the API rejects
- Fixed **Runs → Mark External Dependency Complete** (`runExternalDependencyMarkAsCompleted`) — its sample variables used values the API rejects
- Fixed **Stacks → Dependencies → Batch Create Dependencies** (`stackDependenciesCreateInBatch`) — its sample variables used values the API rejects
- Fixed **Stacks → Dependencies → Batch Create Dependencies** (`stackDependenciesCreateInBatch`) — it selected `dependsOnStackId`, a field the API has deprecated
- Fixed **Stacks → Dependencies → Add References** (`stackDependenciesAddReferences`) — its sample variables used values the API rejects
- Fixed **Stacks → Dependencies → Update References** (`stackDependenciesUpdateReferences`) — its sample variables used values the API rejects
- Fixed **Stacks → Migrate Vendor** (`stackMigrateVendor`) — its sample variables used values the API rejects
- Fixed **Terraform Providers → Create Provider Version** (`terraformProviderVersionCreate`) — its sample variables used values the API rejects
- Fixed **Terraform Providers → Register Platform** (`terraformProviderVersionRegisterPlatform`) — its sample variables used values the API rejects
- Fixed **Terraform Providers → Register Platform V2** (`terraformProviderVersionRegisterPlatformV2`) — its sample variables used values the API rejects
- Fixed **VCS Integrations → GitHub → Update GitHub Integration** (`githubIntegrationUpdate`) — its sample variables used values the API rejects
- Added **Account → List Outgoing IP Addresses** (`outgoingIPAddresses`)
- Added **Account → Get AWS Account ID** (`spaceliftAwsAccountId`)
- Added **Account → Get Default Private Worker Pool Runner Image** (`defaultPrivateWorkerPoolRunnerImage`)
- Added **Account → Get Default Public Worker Pool Runner Image** (`defaultPublicWorkerPoolRunnerImage`)
- Added **Advanced → Account → Id** (`id`)
- Added **Advanced → Account → Name** (`name`)
- Added **Advanced → Account → Type** (`type`)
- Added **Advanced → Account Settings → Accepted Terms And Conditions AI** (`acceptedTermsAndConditionsAI`)
- Added **Advanced → Account Settings → Account Can Be Deleted** (`accountCanBeDeleted`)
- Added **Advanced → Account Settings → Account Can Be Deleted At** (`accountCanBeDeletedAt`)
- Added **Advanced → Account Settings → Account Confirm Delete** (`accountConfirmDelete`)
- Added **Advanced → Account Settings → Account Request Infra Assistant Enabling** (`accountRequestInfraAssistantEnabling`)
- Added **Advanced → Account Settings → Account Set Infra Assistant Build Mode Enabled** (`accountSetInfraAssistantBuildModeEnabled`)
- Added **Advanced → Account Settings → Account Set Infra Assistant Enabled** (`accountSetInfraAssistantEnabled`)
- Added **Advanced → Account Settings → Account Set OAuth Redirect Allowlist** (`accountSetOAuthRedirectAllowlist`)
- Added **Advanced → Account Settings → Account Set OIDCSubject Template** (`accountSetOIDCSubjectTemplate`)
- Added **Advanced → Account Settings → Account Set Spacelift Intelligence Enabled** (`accountSetSpaceliftIntelligenceEnabled`)
- Added **Advanced → Account Settings → Account Toggle APIKey Management From Non Humans** (`accountToggleAPIKeyManagementFromNonHumans`)
- Added **Advanced → Account Settings → Account Toggle Allow Non Root Admin Space Creation** (`accountToggleAllowNonRootAdminSpaceCreation`)
- Added **Advanced → Account Settings → Account Toggle Deletion Mark** (`accountToggleDeletionMark`)
- Added **Advanced → Account Settings → Account Toggle Enforcing MFA** (`accountToggleEnforcingMFA`)
- Added **Advanced → Account Settings → Account Update Authorization Scheme** (`accountUpdateAuthorizationScheme`)
- Added **Advanced → Account Settings → Account Update Awareness Source Survey** (`accountUpdateAwarenessSourceSurvey`)
- Added **Advanced → Account Settings → Account Update Default Worker Pool Runner Images** (`accountUpdateDefaultWorkerPoolRunnerImages`)
- Added **Advanced → Account Settings → Account Update Security Email** (`accountUpdateSecurityEmail`)
- Added **Advanced → Account Settings → Account Update VCSEvent Triggered Runs Limit** (`accountUpdateVCSEventTriggeredRunsLimit`)
- Added **Advanced → Account Settings → Ai Integration Toggle** (`aiIntegrationToggle`)
- Added **Advanced → Account Settings → Allow Non Root Admin Space Creation** (`allowNonRootAdminSpaceCreation`)
- Added **Advanced → Account Settings → Api Keys Manageable By Non Humans** (`apiKeysManageableByNonHumans`)
- Added **Advanced → Account Settings → Api Keys Managed By Non Root Admins** (`apiKeysManagedByNonRootAdmins`)
- Added **Advanced → Account Settings → Audit Trail Retention Days** (`auditTrailRetentionDays`)
- Added **Advanced → Account Settings → Authorization Scheme** (`authorizationScheme`)
- Added **Advanced → Account Settings → Available AIModels** (`availableAIModels`)
- Added **Advanced → Account Settings → Available AIProviders** (`availableAIProviders`)
- Added **Advanced → Account Settings → Awareness Source Survey** (`awarenessSourceSurvey`)
- Added **Advanced → Account Settings → Enforce MFA** (`enforceMFA`)
- Added **Advanced → Account Settings → Has Ansible Stacks** (`hasAnsibleStacks`)
- Added **Advanced → Account Settings → Has SSO** (`hasSSO`)
- Added **Advanced → Account Settings → Infra Assistant Build Mode Enabled** (`infraAssistantBuildModeEnabled`)
- Added **Advanced → Account Settings → Infra Assistant Enabled** (`infraAssistantEnabled`)
- Added **Advanced → Account Settings → Latest Terms And Conditions Version AI** (`latestTermsAndConditionsVersionAI`)
- Added **Advanced → Account Settings → Marked For Deletion** (`markedForDeletion`)
- Added **Advanced → Account Settings → Oauth Redirect Allowed Hosts** (`oauthRedirectAllowedHosts`)
- Added **Advanced → Account Settings → Run Log Retention Days** (`runLogRetentionDays`)
- Added **Advanced → Account Settings → Spacelift Intelligence Enabled** (`spaceliftIntelligenceEnabled`)
- Added **Advanced → Account Settings → Spacelift Intelligence Terms Accepted** (`spaceliftIntelligenceTermsAccepted`)
- Added **Advanced → Account Settings → Spacelift Intelligence Terms Latest Version** (`spaceliftIntelligenceTermsLatestVersion`)
- Added **Advanced → Account Settings → Vcs Event Triggered Runs Limit** (`vcsEventTriggeredRunsLimit`)
- Added **Advanced → Analytics → Group User Event** (`groupUserEvent`)
- Added **Advanced → Analytics → Identify User Event** (`identifyUserEvent`)
- Added **Advanced → Analytics → Page User Event** (`pageUserEvent`)
- Added **Advanced → Analytics → Track User Event** (`trackUserEvent`)
- Added **Advanced → Billing → Available Billing Addons** (`availableBillingAddons`)
- Added **Advanced → Billing → Available Self Service Plans** (`availableSelfServicePlans`)
- Added **Advanced → Billing → Billed Externally** (`billedExternally`)
- Added **Advanced → Billing → Billing Subscription** (`billingSubscription`)
- Added **Advanced → Billing → Billing Subscription Create** (`billingSubscriptionCreate`)
- Added **Advanced → Billing → Billing Subscription Delete** (`billingSubscriptionDelete`)
- Added **Advanced → Billing → Billing Subscription Update Info** (`billingSubscriptionUpdateInfo`)
- Added **Advanced → Billing → Billing Subscription Update Tier** (`billingSubscriptionUpdateTier`)
- Added **Advanced → Billing → Billing Subscription Update V2** (`billingSubscriptionUpdateV2`)
- Added **Advanced → Billing → On Trial Until** (`onTrialUntil`)
- Added **Advanced → Billing → Seats** (`seats`)
- Added **Advanced → Billing → Tier** (`tier`)
- Added **Advanced → Billing → Tier Features** (`tierFeatures`)
- Added **Advanced → Billing → Usage** (`usage`)
- Added **Advanced → Billing → Usage Aspect** (`usageAspect`)
- Added **Advanced → CLI → Cli Confirmation Token** (`cliConfirmationToken`)
- Added **Advanced → Debug → Debug Info** (`debugInfo`)
- Added **Advanced → Feature Flags → Evaluate Feature Flags** (`evaluateFeatureFlags`)
- Added **Advanced → Forms → Complete Generic Form** (`completeGenericForm`)
- Added **Advanced → Forms → Generic Forms List** (`genericFormsList`)
- Added **Advanced → Forms → Is Generic Form Completed** (`isGenericFormCompleted`)
- Added **Advanced → Infrastructure → Installation Id** (`installationId`)
- Added **Advanced → Infrastructure → Message Queue Stats** (`messageQueueStats`)
- Added **Advanced → Infrastructure → Policy Runtime** (`policyRuntime`)
- Added **Advanced → Intent → Intent Chat Conversation** (`intentChatConversation`)
- Added **Advanced → Intent → Intent Chat Conversation Create** (`intentChatConversationCreate`)
- Added **Advanced → Intent → Intent Chat Conversation Delete** (`intentChatConversationDelete`)
- Added **Advanced → Intent → Intent Chat Conversation Update** (`intentChatConversationUpdate`)
- Added **Advanced → Intent → Intent Chat Conversations** (`intentChatConversations`)
- Added **Advanced → Intent → Intent Module Resources** (`intentModuleResources`)
- Added **Advanced → Intent → Intent Policy Input Schema** (`intentPolicyInputSchema`)
- Added **Advanced → Intent → Intent Project** (`intentProject`)
- Added **Advanced → Intent → Intent Project Activate** (`intentProjectActivate`)
- Added **Advanced → Intent → Intent Project Active** (`intentProjectActive`)
- Added **Advanced → Intent → Intent Project Attach Worker Pool** (`intentProjectAttachWorkerPool`)
- Added **Advanced → Intent → Intent Project Config Add** (`intentProjectConfigAdd`)
- Added **Advanced → Intent → Intent Project Config Delete** (`intentProjectConfigDelete`)
- Added **Advanced → Intent → Intent Project Config Update** (`intentProjectConfigUpdate`)
- Added **Advanced → Intent → Intent Project Create** (`intentProjectCreate`)
- Added **Advanced → Intent → Intent Project Delete** (`intentProjectDelete`)
- Added **Advanced → Intent → Intent Project Detach Worker Pool** (`intentProjectDetachWorkerPool`)
- Added **Advanced → Intent → Intent Project Disable** (`intentProjectDisable`)
- Added **Advanced → Intent → Intent Project Enable** (`intentProjectEnable`)
- Added **Advanced → Intent → Intent Project Unlock** (`intentProjectUnlock`)
- Added **Advanced → Intent → Intent Project Update** (`intentProjectUpdate`)
- Added **Advanced → Intent → Intent Projects** (`intentProjects`)
- Added **Advanced → Intent → Intent Resource Dependency Add** (`intentResourceDependencyAdd`)
- Added **Advanced → Intent → Intent Resource Dependency Remove** (`intentResourceDependencyRemove`)
- Added **Advanced → Intent → Intent Resource Operation Review** (`intentResourceOperationReview`)
- Added **Advanced → Intent → Intent Resource State Eject** (`intentResourceStateEject`)
- Added **Advanced → Intent → Search Intent Projects** (`searchIntentProjects`)
- Added **Advanced → Intent → Search Intent Projects Suggestions** (`searchIntentProjectsSuggestions`)
- Added **Advanced → Intent → Search Intent Resources** (`searchIntentResources`)
- Added **Advanced → Intent → Search Intent Resources Operations** (`searchIntentResourcesOperations`)
- Added **Advanced → Intent → Search Intent Resources Operations Suggestions** (`searchIntentResourcesOperationsSuggestions`)
- Added **Advanced → Intent → Search Intent Resources Suggestions** (`searchIntentResourcesSuggestions`)
- Added **Advanced → Intent → Stack Create From Intent** (`stackCreateFromIntent`)
- Added **Advanced → Notifications → Dismiss All Notifications** (`dismissAllNotifications`)
- Added **Advanced → Notifications → Dismiss Notification Group** (`dismissNotificationGroup`)
- Added **Advanced → Notifications → Dismiss Notifications** (`dismissNotifications`)
- Added **Advanced → Notifications → Notification Count** (`notificationCount`)
- Added **Advanced → Notifications → Search Notifications** (`searchNotifications`)
- Added **Advanced → OAuth → Oauth Redirect** (`oauthRedirect`)
- Added **Advanced → OAuth → Oauth Token** (`oauthToken`)
- Added **Advanced → OAuth → Oauth User** (`oauthUser`)
- Added **Advanced → OAuth → Slack Oauth Redirect** (`slackOauthRedirect`)
- Added **Advanced → Search Suggestions → API Key Sessions With Access To Space Suggestions** (`apiKeySessionsWithAccessToSpaceSuggestions`)
- Added **Advanced → Search Suggestions → API Keys With Access To Space Suggestions** (`apiKeysWithAccessToSpaceSuggestions`)
- Added **Advanced → Search Suggestions → Open Tofu Migrate Suggestions** (`openTofuMigrateSuggestions`)
- Added **Advanced → Search Suggestions → Open Tofu Migration Queue Search Suggestions** (`openTofuMigrationQueueSearchSuggestions`)
- Added **Advanced → Search Suggestions → Search Ansible Hosts Suggestions** (`searchAnsibleHostsSuggestions`)
- Added **Advanced → Search Suggestions → Search Ansible Tasks Suggestions** (`searchAnsibleTasksSuggestions`)
- Added **Advanced → Search Suggestions → Search Audit Trail Entries Suggestions** (`searchAuditTrailEntriesSuggestions`)
- Added **Advanced → Search Suggestions → Search Blueprint Versioned Groups Suggestions** (`searchBlueprintVersionedGroupsSuggestions`)
- Added **Advanced → Search Suggestions → Search Blueprints Suggestions** (`searchBlueprintsSuggestions`)
- Added **Advanced → Search Suggestions → Search Contexts Suggestions** (`searchContextsSuggestions`)
- Added **Advanced → Search Suggestions → Search Intent Projects Suggestions** (`searchIntentProjectsSuggestions`)
- Added **Advanced → Search Suggestions → Search Intent Resources Operations Suggestions** (`searchIntentResourcesOperationsSuggestions`)
- Added **Advanced → Search Suggestions → Search Intent Resources Suggestions** (`searchIntentResourcesSuggestions`)
- Added **Advanced → Search Suggestions → Search Managed Entities Suggestions** (`searchManagedEntitiesSuggestions`)
- Added **Advanced → Search Suggestions → Search Modules Suggestions** (`searchModulesSuggestions`)
- Added **Advanced → Search Suggestions → Search Named Webhooks Integrations Suggestions** (`searchNamedWebhooksIntegrationsSuggestions`)
- Added **Advanced → Search Suggestions → Search Notifications Suggestions** (`searchNotificationsSuggestions`)
- Added **Advanced → Search Suggestions → Search Policies Suggestions** (`searchPoliciesSuggestions`)
- Added **Advanced → Search Suggestions → Search Policy Templates Suggestions** (`searchPolicyTemplatesSuggestions`)
- Added **Advanced → Search Suggestions → Search Repo Files Suggestions** (`searchRepoFilesSuggestions`)
- Added **Advanced → Search Suggestions → Search Repos Suggestions** (`searchReposSuggestions`)
- Added **Advanced → Search Suggestions → Search Revisions Suggestions** (`searchRevisionsSuggestions`)
- Added **Advanced → Search Suggestions → Search Runs Suggestions** (`searchRunsSuggestions`)
- Added **Advanced → Search Suggestions → Search Space Contexts Suggestions** (`searchSpaceContextsSuggestions`)
- Added **Advanced → Search Suggestions → Search Space Policies Suggestions** (`searchSpacePoliciesSuggestions`)
- Added **Advanced → Search Suggestions → Search Spaces Suggestions** (`searchSpacesSuggestions`)
- Added **Advanced → Search Suggestions → Search Stacks Suggestions** (`searchStacksSuggestions`)
- Added **Advanced → Search Suggestions → Search Tofu Workspaces Suggestions** (`searchTofuWorkspacesSuggestions`)
- Added **Advanced → Search Suggestions → Search VCSIntegrations Suggestions** (`searchVCSIntegrationsSuggestions`)
- Added **Advanced → Search Suggestions → Search Worker Pools Suggestions** (`searchWorkerPoolsSuggestions`)
- Added **Advanced → Search Suggestions → Stacks With Access To Space Suggestions** (`stacksWithAccessToSpaceSuggestions`)
- Added **Advanced → Search Suggestions → User Groups With Access To Space Suggestions** (`userGroupsWithAccessToSpaceSuggestions`)
- Added **Advanced → Search Suggestions → User Sessions With Access To Space Suggestions** (`userSessionsWithAccessToSpaceSuggestions`)
- Added **Advanced → Search Suggestions → Users With Access To Space Suggestions** (`usersWithAccessToSpaceSuggestions`)
- Added **Advanced → Sessions and Security Keys → Security Email** (`securityEmail`)
- Added **Advanced → Sessions and Security Keys → Security Key Delete** (`securityKeyDelete`)
- Added **Advanced → Sessions and Security Keys → Security Keys** (`securityKeys`)
- Added **Advanced → Sessions and Security Keys → Session Delete** (`sessionDelete`)
- Added **Advanced → Sessions and Security Keys → Session Delete All** (`sessionDeleteAll`)
- Added **Advanced → Sessions and Security Keys → Sessions** (`sessions`)
- Added **Advanced → Sessions and Security Keys → User Security Key Delete All** (`userSecurityKeyDeleteAll`)
- Added **Advanced → Slack → Github App Create From Manifest** (`githubAppCreateFromManifest`)
- Added **Advanced → Slack → Github App Generate Manifest** (`githubAppGenerateManifest`)
- Added **Advanced → Slack → Slack App Config** (`slackAppConfig`)
- Added **Advanced → Slack → Slack App Config Delete** (`slackAppConfigDelete`)
- Added **Advanced → Slack → Slack App Config Set** (`slackAppConfigSet`)
- Added **Advanced → Slack → Slack App Manifest** (`slackAppManifest`)
- Added **Advanced → Slack → Slack Integration** (`slackIntegration`)
- Added **Advanced → SSO → Create Oauth Client For SCIM** (`createOauthClientForSCIM`)
- Added **Advanced → SSO → Delete Oauth Client For SCIM** (`deleteOauthClientForSCIM`)
- Added **Advanced → SSO → Oidc Create** (`oidcCreate`)
- Added **Advanced → SSO → Oidc Delete** (`oidcDelete`)
- Added **Advanced → SSO → Oidc Settings** (`oidcSettings`)
- Added **Advanced → SSO → Oidc Subject Template** (`oidcSubjectTemplate`)
- Added **Advanced → SSO → Oidc Update** (`oidcUpdate`)
- Added **Advanced → SSO → Reset Oauth Client For SCIM** (`resetOauthClientForSCIM`)
- Added **Advanced → SSO → Saml Create** (`samlCreate`)
- Added **Advanced → SSO → Saml Delete** (`samlDelete`)
- Added **Advanced → SSO → Saml Settings** (`samlSettings`)
- Added **Advanced → SSO → Saml Update** (`samlUpdate`)
- Added **Advanced → SSO → Scim Settings** (`scimSettings`)
- Added **Advanced → UI State → Ui Config Get** (`uiConfigGet`)
- Added **Advanced → UI State → Ui Config Store** (`uiConfigStore`)
- Added **Advanced → User Guide → Active User Guide Progress** (`activeUserGuideProgress`)
- Added **Advanced → User Guide → Completed User Guides** (`completedUserGuides`)
- Added **Advanced → User Guide → Onboarding Progress** (`onboardingProgress`)
- Added **Advanced → User Guide → User Guide** (`userGuide`)
- Added **Advanced → User Guide → User Guide Abandon** (`userGuideAbandon`)
- Added **Advanced → User Guide → User Guide Certification Create** (`userGuideCertificationCreate`)
- Added **Advanced → User Guide → User Guide Certification List** (`userGuideCertificationList`)
- Added **Advanced → User Guide → User Guide Certification Update** (`userGuideCertificationUpdate`)
- Added **Advanced → User Guide → User Guide Chapter** (`userGuideChapter`)
- Added **Advanced → User Guide → User Guide Chapters** (`userGuideChapters`)
- Added **Advanced → User Guide → User Guide Complete** (`userGuideComplete`)
- Added **Advanced → User Guide → User Guide Group** (`userGuideGroup`)
- Added **Advanced → User Guide → User Guide Groups** (`userGuideGroups`)
- Added **Advanced → User Guide → User Guide Move To Next Step** (`userGuideMoveToNextStep`)
- Added **Advanced → User Guide → User Guide Move To Previous Step** (`userGuideMoveToPreviousStep`)
- Added **Advanced → User Guide → User Guide Restart** (`userGuideRestart`)
- Added **Advanced → User Guide → User Guide Start** (`userGuideStart`)
- Added **Advanced → User Guide → User Guides** (`userGuides`)
- Added **Advanced → Viewer → Viewer** (`viewer`)
- Added **API Keys → Reset API Key** (`apiKeyReset`)
- Added **Blob Storage → Get Blob Storage Integration** (`blobStorageIntegration`)
- Added **Blob Storage → Create Blob Storage Integration** (`blobStorageIntegrationCreate`)
- Added **Blob Storage → Update Blob Storage Integration** (`blobStorageIntegrationUpdate`)
- Added **Blob Storage → Delete Blob Storage Integration** (`blobStorageIntegrationDelete`)
- Added **Flows → Get Flows Integration** (`flowsIntegration`)
- Added **Flows → List Flows Instances** (`flowsInstances`)
- Added **Flows → List Flows Projects** (`flowsProjects`)
- Added **Flows → Batch Create Project Role Bindings** (`flowsProjectRoleBindingBatchCreate`)
- Added **Flows → Delete Project Role Binding** (`flowsProjectRoleBindingDelete`)
- Added **Managed Users → Unlink Identity Federation** (`identityFederationUnlink`)
- Added **OpenTofu Migration → Check OpenTofu Features** (`openTofuFeatures`)
- Added **OpenTofu Migration → Migration Counts** (`openTofuMigrationCounts`)
- Added **OpenTofu Migration → Search Migratable Stacks** (`openTofuMigrateSearch`)
- Added **OpenTofu Migration → Search Migration Queue** (`openTofuMigrationQueueSearch`)
- Added **OpenTofu Migration → Migrate Stacks** (`openTofuMigrate`)
- Added **OpenTofu Migration → Clean Migration Queue** (`openTofuMigrationQueueClean`)
- Added **Origin Integration → Get Origin Integration** (`originIntegration`)
- Added **Origin Integration → Get Install URL** (`originIntegrationInstallURL`)
- Added **Origin Integration → Update Origin Integration** (`originIntegrationUpdate`)
- Added **Origin Integration → Delete Origin Integration** (`originIntegrationDelete`)
- Added **Personal API Keys → Create Personal API Key** (`personalApiKeyCreate`)
- Added **Personal API Keys → Disable Personal API Key** (`personalApiKeyDisable`)
- Added **Personal API Keys → Enable Personal API Key** (`personalApiKeyEnable`)
- Added **Runs → Trigger Run Batch** (`runTriggerBatch`)
- Added **Runs → Get Run** (`run`)
- Added **Runs → Search Runs** (`searchRuns`)
- Added **Runs → Run Minutes Usage** (`runMinutesUsage`)
- Added **Scans → Search Scans** (`searchScans`)
- Added **Scans → Get Scan** (`scan`)
- Added **Scans → Create Scan** (`scanCreate`)
- Added **Scans → Update Scan** (`scanUpdate`)
- Added **Scans → Delete Scan** (`scanDelete`)
- Added **Scans → Trigger Scan Run** (`scanRunTrigger`)
- Added **Scans → Get Scan Run** (`scanRun`)
- Added **Scans → Cancel Scan Run** (`scanRunCancel`)
- Added **Scans → Stop Scan Run** (`scanRunStop`)
- Added **Scans → List Scan Provider Schemas** (`scanProviderSchemas`)
- Added **Scans → Search Scans Suggestions** (`searchScansSuggestions`)
- Added **Spaces → Search Spaces** (`searchSpaces`)
- Added **Spaces → Search Space Contexts** (`searchSpaceContexts`)
- Added **Spaces → Search Space Policies** (`searchSpacePolicies`)
- Added **Spaces → Stacks With Access To Space** (`stacksWithAccessToSpace`)
- Added **Spaces → Users With Access To Space** (`usersWithAccessToSpace`)
- Added **Spaces → User Groups With Access To Space** (`userGroupsWithAccessToSpace`)
- Added **Spaces → API Keys With Access To Space** (`apiKeysWithAccessToSpace`)
- Added **Spaces → API Key Sessions With Access To Space** (`apiKeySessionsWithAccessToSpace`)
- Added **Spaces → User Sessions With Access To Space** (`userSessionsWithAccessToSpace`)
- Added **Stacks → Migrate Vendor For All Stacks** (`stackMigrateVendorAll`)
- Added **Templates → Deployments → Search Deployments** (`searchTemplateDeployments`)
- Added **Templates → Deployments → Get Deployment** (`templateDeployment`)
- Added **Templates → Deployments → Create Deployment** (`templateDeploymentCreate`)
- Added **Templates → Deployments → Update Deployment** (`templateDeploymentUpdate`)
- Added **Templates → Deployments → Rollback Deployment** (`templateDeploymentRollback`)
- Added **Templates → Deployments → Delete Deployment** (`templateDeploymentDelete`)
- Added **Templates → Versions → Create Version** (`templateVersionCreate`)
- Added **Templates → Versions → Update Version** (`templateVersionUpdate`)
- Added **Templates → Versions → Delete Version** (`templateVersionDelete`)
- Added **Templates → Versions → Deprecate Version** (`templateVersionDeprecate`)
- Added **Templates → Versions → Parse Template** (`templateVersionParseTemplate`)
- Added **Templates → Search Templates** (`searchTemplates`)
- Added **Templates → Get Template** (`template`)
- Added **Templates → Create Template** (`templateCreate`)
- Added **Templates → Update Template** (`templateUpdate`)
- Added **Templates → Delete Template** (`templateDelete`)
- Added **Templates → Get Template Schema** (`templateSchema`)
- Added **Templates → Search Templates Suggestions** (`searchTemplatesSuggestions`)
- Added **Terraform Providers → Upload Version Docs** (`terraformProviderVersionUploadDocs`)
- Added **Terraform Providers → Verify Version** (`terraformProviderVersionVerify`)
- Added **Tofu Workspaces → Unlock Tofu Workspace** (`tofuWorkspaceUnlock`)
- Added **Worker Pools → Get Worker Usage** (`workerUsage`)

## 2026-03-11

- Added **AI Integrations → List AI Integrations** (`aiIntegrations`)
- Added **AI Integrations → Get AI Integration** (`aiIntegration`)
- Added **AI Integrations → Create AI Integration** (`aiIntegrationCreate`)
- Added **AI Integrations → Update AI Integration** (`aiIntegrationUpdate`)
- Added **AI Integrations → Delete AI Integration** (`aiIntegrationDelete`)
- Added **Ansible → List Ansible Hosts** (`ansibleHosts`)
- Added **Ansible → Get Ansible Host** (`ansibleHost`)
- Added **Ansible → List Ansible Tasks** (`ansibleTasks`)
- Added **Ansible → Search Ansible Hosts** (`searchAnsibleHosts`)
- Added **Ansible → Search Ansible Tasks** (`searchAnsibleTasks`)
- Added **API Keys → List API Keys** (`apiKeys`)
- Added **API Keys → Get API Key** (`apiKey`)
- Added **API Keys → Create API Key** (`apiKeyCreate`)
- Added **API Keys → Update API Key** (`apiKeyUpdate`)
- Added **API Keys → Delete API Key** (`apiKeyDelete`)
- Added **API Keys → Enable Non-Root Managers** (`apiKeyEnableNonRootManagers`)
- Added **Audit Trail → Get Audit Trail Webhook** (`auditTrailWebhook`)
- Added **Audit Trail → Search Audit Trail Entries** (`searchAuditTrailEntries`)
- Added **Audit Trail → Set Audit Trail Webhook** (`auditTrailSetWebhook`)
- Added **Audit Trail → Delete Audit Trail Webhook** (`auditTrailDeleteWebhook`)
- Added **Audit Trail → Set Webhook Headers** (`auditTrailWebhookSetHeaders`)
- Added **Audit Trail → Delete Webhook Headers** (`auditTrailWebhookDeleteHeaders`)
- Added **Auth → Get Token** (`apiKeyUser`)
- Added **Auth → Logout** (`logout`)
- Added **Blueprints → Deployments → List Deployments** (`searchBlueprintDeployments`)
- Added **Blueprints → Deployments → Get Deployment** (`blueprintDeployment`)
- Added **Blueprints → Deployments → Create Deployment** (`blueprintDeploymentCreate`)
- Added **Blueprints → Deployments → Update Deployment** (`blueprintDeploymentUpdate`)
- Added **Blueprints → Deployments → Delete Deployment** (`blueprintDeploymentDelete`)
- Added **Blueprints → Deployments → Attach to Integration** (`blueprintAttachToIntegration`)
- Added **Blueprints → Deployments → Detach Integration** (`blueprintDetachIntegration`)
- Added **Blueprints → Groups → Create Blueprint With Group** (`blueprintWithGroupCreate`)
- Added **Blueprints → Groups → Update Blueprint With Group** (`blueprintWithGroupUpdate`)
- Added **Blueprints → Groups → Delete Blueprint With Group** (`blueprintWithGroupDelete`)
- Added **Blueprints → Versioned Groups → List Versioned Groups** (`searchBlueprintVersionedGroups`)
- Added **Blueprints → Versioned Groups → Get Versioned Group** (`blueprintVersionedGroup`)
- Added **Blueprints → Versioned Groups → Create Versioned Group** (`blueprintVersionedGroupCreate`)
- Added **Blueprints → Versioned Groups → Update Versioned Group** (`blueprintVersionedGroupUpdate`)
- Added **Blueprints → Versioned Groups → Delete Versioned Group** (`blueprintVersionedGroupDelete`)
- Added **Blueprints → Versions → Create Version** (`blueprintVersionCreate`)
- Added **Blueprints → Versions → Update Version** (`blueprintVersionUpdate`)
- Added **Blueprints → Versions → Delete Version** (`blueprintVersionDelete`)
- Added **Blueprints → Versions → Create Stack from Version** (`blueprintCreateStack`)
- Added **Blueprints → Versions → Parse Blueprint Version Template** (`blueprintVersionParseTemplate`)
- Added **Blueprints → List Blueprints** (`searchBlueprints`)
- Added **Blueprints → Get Blueprint** (`blueprint`)
- Added **Blueprints → Create Blueprint** (`blueprintCreate`)
- Added **Blueprints → Update Blueprint** (`blueprintUpdate`)
- Added **Blueprints → Delete Blueprint** (`blueprintDelete`)
- Added **Blueprints → Create Stack from Blueprint** (`blueprintCreateStack`)
- Added **Blueprints → Clone Blueprint** (`blueprintClone`)
- Added **Blueprints → Parse Template** (`blueprintParseTemplate`)
- Added **Blueprints → Get Blueprint Schema** (`blueprintSchema`)
- Added **Blueprints → Get Template Schema** (`templateSchema`)
- Added **Cloud Integrations → AWS → List AWS Integrations** (`awsIntegrations`)
- Added **Cloud Integrations → AWS → Get AWS Integration** (`awsIntegration`)
- Added **Cloud Integrations → AWS → Create AWS Integration** (`awsIntegrationCreate`)
- Added **Cloud Integrations → AWS → Update AWS Integration** (`awsIntegrationUpdate`)
- Added **Cloud Integrations → AWS → Delete AWS Integration** (`awsIntegrationDelete`)
- Added **Cloud Integrations → AWS → Attach AWS Integration** (`awsIntegrationAttach`)
- Added **Cloud Integrations → AWS → Detach AWS Integration** (`awsIntegrationDetach`)
- Added **Cloud Integrations → AWS → Get AWS Integration by Name** (`awsIntegrationByName`)
- Added **Cloud Integrations → AWS → List Enabled AWS Integrations** (`awsIntegrationsEnabled`)
- Added **Cloud Integrations → AWS → Attach to Stack** (`stackIntegrationAwsCreate`)
- Added **Cloud Integrations → AWS → Detach from Stack** (`stackIntegrationAwsDelete`)
- Added **Cloud Integrations → AWS → Update Attachment** (`awsIntegrationAttachmentUpdate`)
- Added **Cloud Integrations → AWS → Attach to Intent Project** (`awsIntegrationIntentProjectAttach`)
- Added **Cloud Integrations → AWS → Detach from Intent Project** (`awsIntegrationIntentProjectDetach`)
- Added **Cloud Integrations → Azure → List Azure Integrations** (`azureIntegrations`)
- Added **Cloud Integrations → Azure → Get Azure Integration** (`azureIntegration`)
- Added **Cloud Integrations → Azure → Create Azure Integration** (`azureIntegrationCreate`)
- Added **Cloud Integrations → Azure → Update Azure Integration** (`azureIntegrationUpdate`)
- Added **Cloud Integrations → Azure → Delete Azure Integration** (`azureIntegrationDelete`)
- Added **Cloud Integrations → Azure → Attach Azure Integration** (`azureIntegrationAttach`)
- Added **Cloud Integrations → Azure → Detach Azure Integration** (`azureIntegrationDetach`)
- Added **Cloud Integrations → Azure → Get Azure Integration by Name** (`azureIntegrationByName`)
- Added **Cloud Integrations → Azure → List Enabled Azure Integrations** (`azureIntegrationsEnabled`)
- Added **Cloud Integrations → Azure → Update Attachment** (`azureIntegrationAttachmentUpdate`)
- Added **Contexts → List Contexts** (`contexts`)
- Added **Contexts → Get Context** (`context`)
- Added **Contexts → Create Context** (`contextCreateV2`)
- Added **Contexts → Update Context** (`contextUpdateV2`)
- Added **Contexts → Delete Context** (`contextDelete`)
- Added **Contexts → Attach Context** (`contextAttach`)
- Added **Contexts → Detach Context** (`contextDetach`)
- Added **Contexts → Add Config** (`contextConfigAdd`)
- Added **Contexts → Delete Config** (`contextConfigDelete`)
- Added **Contexts → Search Contexts** (`searchContexts`)
- Added **Contexts → Attachable Contexts in Space** (`attachableContextsInSpace`)
- Added **Drift Detection → Create Drift Detection** (`stackIntegrationDriftDetectionCreate`)
- Added **Drift Detection → Update Drift Detection** (`stackIntegrationDriftDetectionUpdate`)
- Added **Drift Detection → Delete Drift Detection** (`stackIntegrationDriftDetectionDelete`)
- Added **External Integrations → List External Integrations** (`externalIntegrations`)
- Added **External Integrations → Get External Integration** (`externalIntegration`)
- Added **External Integrations → Create External Integration** (`externalIntegrationCreate`)
- Added **External Integrations → Delete External Integration** (`externalIntegrationDelete`)
- Added **GPG Keys → List GPG Keys** (`gpgKeys`)
- Added **GPG Keys → Create GPG Key** (`gpgKeyCreate`)
- Added **GPG Keys → Update GPG Key** (`gpgKeyUpdate`)
- Added **GPG Keys → Revoke GPG Key** (`gpgKeyRevoke`)
- Added **Managed Users → User Groups → List User Groups** (`managedUserGroups`)
- Added **Managed Users → User Groups → Get User Group** (`managedUserGroup`)
- Added **Managed Users → User Groups → Create User Group** (`managedUserGroupCreateV2`)
- Added **Managed Users → User Groups → Update User Group** (`managedUserGroupUpdateV2`)
- Added **Managed Users → User Groups → Delete User Group** (`managedUserGroupDelete`)
- Added **Managed Users → User Groups → List User Group Integrations** (`managedUserGroupIntegrations`)
- Added **Managed Users → User Groups → Get User Group Integration** (`managedUserGroupIntegration`)
- Added **Managed Users → User Groups → Create User Group Integration** (`managedUserGroupIntegrationCreate`)
- Added **Managed Users → User Groups → Update User Group Integration** (`managedUserGroupIntegrationUpdate`)
- Added **Managed Users → User Groups → Delete User Group Integration** (`managedUserGroupIntegrationDelete`)
- Added **Managed Users → User Groups → List Available Groups** (`availableUserGroups`)
- Added **Managed Users → List Managed Users** (`managedUsers`)
- Added **Managed Users → Get Managed User** (`managedUser`)
- Added **Managed Users → Invite Managed User** (`managedUserInviteV2`)
- Added **Managed Users → Request Invite** (`managedUserRequestInvite`)
- Added **Managed Users → Resend Invite** (`managedUserResendInvite`)
- Added **Managed Users → Update Managed User** (`managedUserUpdate`)
- Added **Managed Users → Delete Managed User** (`managedUserDelete`)
- Added **Managed Users → Request Role Change** (`managedUserRequestRoleChange`)
- Added **Modules → Versions → Create Version** (`versionCreate`)
- Added **Modules → Versions → Push Version** (`versionPush`)
- Added **Modules → Versions → Trigger Version** (`versionTrigger`)
- Added **Modules → Versions → Annotate Version** (`versionAnnotate`)
- Added **Modules → Versions → Yank Version** (`versionYank`)
- Added **Modules → Versions → Delete Version** (`versionDelete`)
- Added **Modules → Versions → Propose Local Workspace** (`versionProposeLocalWorkspace`)
- Added **Modules → List Modules** (`modules`)
- Added **Modules → Get Module** (`module`)
- Added **Modules → Create Module** (`moduleCreate`)
- Added **Modules → Update Module** (`moduleUpdate`)
- Added **Modules → Delete Module** (`moduleDelete`)
- Added **Modules → Publish Module** (`modulePublish`)
- Added **Modules → Enable Module** (`moduleEnable`)
- Added **Modules → Disable Module** (`moduleDisable`)
- Added **Modules → Share Module** (`moduleShare`)
- Added **Modules → Search Modules** (`searchModules`)
- Added **Modules → List Shared Modules** (`sharedModules`)
- Added **Modules → List Shared Modules V2** (`sharedModulesV2`)
- Added **Modules → Update Module V2** (`moduleUpdateV2`)
- Added **Plugins → Templates → List Plugin Templates** (`pluginTemplates`)
- Added **Plugins → Templates → Get Plugin Template** (`pluginTemplate`)
- Added **Plugins → Templates → Create Plugin Template** (`pluginTemplateCreate`)
- Added **Plugins → Templates → Delete Plugin Template** (`pluginTemplateDelete`)
- Added **Plugins → List Plugins** (`plugins`)
- Added **Plugins → Get Plugin** (`plugin`)
- Added **Plugins → Install Plugin** (`pluginInstall`)
- Added **Plugins → Update Plugin** (`pluginUpdate`)
- Added **Plugins → Delete Plugin** (`pluginDelete`)
- Added **Policies → List Policies** (`policies`)
- Added **Policies → Get Policy** (`policy`)
- Added **Policies → Create Policy** (`policyCreatev2`)
- Added **Policies → Update Policy** (`policyUpdatev2`)
- Added **Policies → Delete Policy** (`policyDelete`)
- Added **Policies → Attach Policy** (`policyAttach`)
- Added **Policies → Detach Policy** (`policyDetach`)
- Added **Policies → Simulate Policy** (`policySimulatev2`)
- Added **Policies → Search Policies** (`searchPolicies`)
- Added **Policies → Get Policy Template** (`policyTemplate`)
- Added **Policies → Search Policy Templates** (`searchPolicyTemplates`)
- Added **Policies → Attachable Policies in Space** (`attachablePoliciesInSpace`)
- Added **Policies → Autoattachment for Labels** (`autoattachmentForLabels`)
- Added **Policies → Attach to Intent Project** (`policyIntentProjectAttach`)
- Added **Policies → Detach from Intent Project** (`policyIntentProjectDetach`)
- Added **Repos → List Repos** (`searchRepos`)
- Added **Repos → Get Repo** (`repo`)
- Added **Repos → Create Repo** (`repoCreate`)
- Added **Repos → Update Repo** (`repoUpdate`)
- Added **Repos → Delete Repo** (`repoDelete`)
- Added **Repos → List Revisions** (`searchRevisions`)
- Added **Repos → Get Revision** (`revision`)
- Added **Repos → Create Revision** (`revisionCreate`)
- Added **Repos → Search Repo Files** (`searchRepoFiles`)
- Added **Repos → Get File History** (`repoFileHistory`)
- Added **Repos → List Space-Scoped Repos** (`repos`)
- Added **Resources → Search Resources** (`searchManagedEntities`)
- Added **Resources → Search Resources by Stack** (`searchManagedEntitiesGroupByStacks`)
- Added **Resources → Search Resources by Type** (`searchManagedEntitiesGroupByTypes`)
- Added **Resources → Search Resources by Provider** (`searchManagedEntitiesGroupByProviders`)
- Added **Roles → API Key Role Bindings → Get API Key Role Binding** (`apiKeyRoleBinding`)
- Added **Roles → API Key Role Bindings → Create API Key Role Binding** (`apiKeyRoleBindingCreate`)
- Added **Roles → API Key Role Bindings → Batch Create API Key Role Bindings** (`apiKeyRoleBindingBatchCreate`)
- Added **Roles → API Key Role Bindings → Update API Key Role Binding** (`apiKeyRoleBindingUpdate`)
- Added **Roles → API Key Role Bindings → Delete API Key Role Binding** (`apiKeyRoleBindingDelete`)
- Added **Roles → Stack Role Bindings → Get Stack Role Binding** (`stackRoleBinding`)
- Added **Roles → Stack Role Bindings → Create Stack Role Binding** (`stackRoleBindingCreate`)
- Added **Roles → Stack Role Bindings → Batch Create Stack Role Bindings** (`stackRoleBindingBatchCreate`)
- Added **Roles → Stack Role Bindings → Update Stack Role Binding** (`stackRoleBindingUpdate`)
- Added **Roles → Stack Role Bindings → Delete Stack Role Binding** (`stackRoleBindingDelete`)
- Added **Roles → User Group Role Bindings → Get User Group Role Binding** (`userGroupRoleBinding`)
- Added **Roles → User Group Role Bindings → Create User Group Role Binding** (`userGroupRoleBindingCreate`)
- Added **Roles → User Group Role Bindings → Batch Create User Group Role Bindings** (`userGroupRoleBindingBatchCreate`)
- Added **Roles → User Group Role Bindings → Update User Group Role Binding** (`userGroupRoleBindingUpdate`)
- Added **Roles → User Group Role Bindings → Delete User Group Role Binding** (`userGroupRoleBindingDelete`)
- Added **Roles → User Role Bindings → Get User Role Binding** (`userRoleBinding`)
- Added **Roles → User Role Bindings → Create User Role Binding** (`userRoleBindingCreate`)
- Added **Roles → User Role Bindings → Batch Create User Role Bindings** (`userRoleBindingBatchCreate`)
- Added **Roles → User Role Bindings → Update User Role Binding** (`userRoleBindingUpdate`)
- Added **Roles → User Role Bindings → Delete User Role Binding** (`userRoleBindingDelete`)
- Added **Roles → List Roles** (`roles`)
- Added **Roles → Get Role** (`role`)
- Added **Roles → Create Role** (`roleCreate`)
- Added **Roles → Update Role** (`roleUpdate`)
- Added **Roles → Delete Role** (`roleDelete`)
- Added **Runs → Trigger Run** (`runTrigger`)
- Added **Runs → Confirm Run** (`runConfirm`)
- Added **Runs → Discard Run** (`runDiscard`)
- Added **Runs → Cancel Run** (`runCancel`)
- Added **Runs → Retry Run** (`runRetry`)
- Added **Runs → Promote Run** (`runPromote`)
- Added **Runs → Review Run** (`runReview`)
- Added **Runs → Comment Run** (`runComment`)
- Added **Runs → Stop Run** (`runStop`)
- Added **Runs → Kill Run** (`runKill`)
- Added **Runs → Discard All Runs** (`runDiscardAll`)
- Added **Runs → Targeted Replan** (`runTargetedReplan`)
- Added **Runs → Get Run Stack** (`runStack`)
- Added **Runs → Create Task** (`taskCreate`)
- Added **Runs → Set Run Priority** (`runPrioritySet`)
- Added **Runs → Prioritize Run** (`runPrioritizeSet`)
- Added **Runs → Mark External Dependency Complete** (`runExternalDependencyMarkAsCompleted`)
- Added **Runs → Upload Local Workspace** (`uploadLocalWorkspace`)
- Added **Runs → Propose Local Workspace Run** (`runProposeLocalWorkspace`)
- Added **Runs → Propose With Overrides** (`proposeWithOverrides`)
- Added **Runs → Create Run Resource** (`runResourceCreate`)
- Added **Saved Filters → List Saved Filters** (`savedFilters`)
- Added **Saved Filters → Get Saved Filter** (`savedFilter`)
- Added **Saved Filters → Create Saved Filter** (`savedFilterCreate`)
- Added **Saved Filters → Update Saved Filter** (`savedFilterUpdate`)
- Added **Saved Filters → Delete Saved Filter** (`savedFilterDelete`)
- Added **Spaces → Attachable Resources → Attachable AWS Integrations** (`attachableAwsIntegrationsInSpace`)
- Added **Spaces → Attachable Resources → Attachable Azure Integrations** (`attachableAzureIntegrationsInSpace`)
- Added **Spaces → Attachable Resources → Attachable Contexts** (`attachableContextsInSpace`)
- Added **Spaces → Attachable Resources → Attachable External Integrations** (`attachableExternalIntegrationInSpace`)
- Added **Spaces → Attachable Resources → Attachable Policies** (`attachablePoliciesInSpace`)
- Added **Spaces → Attachable Resources → Attachable VCS Integrations** (`attachableVCSIntegrationsForProviderInSpace`)
- Added **Spaces → Attachable Resources → Attachable Worker Pools** (`attachableWorkerPoolsInSpace`)
- Added **Spaces → List Spaces** (`spaces`)
- Added **Spaces → Get Space** (`space`)
- Added **Spaces → Create Space** (`spaceCreate`)
- Added **Spaces → Update Space** (`spaceUpdate`)
- Added **Spaces → Delete Space** (`spaceDelete`)
- Added **Spaces → Get Integrations Count** (`integrationsCount`)
- Added **Spaces → Get Metrics** (`metrics`)
- Added **Spaces → Get Metrics Range** (`metricsRange`)
- Added **Stacks → Dependencies → Create Dependency** (`stackDependencyCreate`)
- Added **Stacks → Dependencies → Update Dependency** (`stackDependencyUpdate`)
- Added **Stacks → Dependencies → Delete Dependency** (`stackDependencyDelete`)
- Added **Stacks → Dependencies → Add Reference** (`stackDependenciesAddReference`)
- Added **Stacks → Dependencies → Update Reference** (`stackDependenciesUpdateReference`)
- Added **Stacks → Dependencies → Delete Reference** (`stackDependenciesDeleteReference`)
- Added **Stacks → Dependencies → Batch Create Dependencies** (`stackDependenciesCreateInBatch`)
- Added **Stacks → Dependencies → Add References** (`stackDependenciesAddReferences`)
- Added **Stacks → Dependencies → Update References** (`stackDependenciesUpdateReferences`)
- Added **Stacks → Dependencies → Delete References** (`stackDependenciesDeleteReferences`)
- Added **Stacks → Scheduled Deletes → Create Scheduled Delete** (`stackScheduledDeleteCreate`)
- Added **Stacks → Scheduled Deletes → Update Scheduled Delete** (`stackScheduledDeleteUpdate`)
- Added **Stacks → Scheduled Deletes → Delete Scheduled Delete** (`stackScheduledDeleteDelete`)
- Added **Stacks → Scheduled Runs → Create Scheduled Run** (`stackScheduledRunCreate`)
- Added **Stacks → Scheduled Runs → Update Scheduled Run** (`stackScheduledRunUpdate`)
- Added **Stacks → Scheduled Runs → Delete Scheduled Run** (`stackScheduledRunDelete`)
- Added **Stacks → Scheduled Tasks → Create Scheduled Task** (`stackScheduledTaskCreate`)
- Added **Stacks → Scheduled Tasks → Update Scheduled Task** (`stackScheduledTaskUpdate`)
- Added **Stacks → Scheduled Tasks → Delete Scheduled Task** (`stackScheduledTaskDelete`)
- Added **Stacks → Webhooks → Create Stack Webhook** (`webhooksIntegrationCreate`)
- Added **Stacks → Webhooks → Update Stack Webhook** (`webhooksIntegrationUpdate`)
- Added **Stacks → Webhooks → Delete Stack Webhook** (`webhooksIntegrationDelete`)
- Added **Stacks → List Stacks** (`stacks`)
- Added **Stacks → Get Stack** (`stack`)
- Added **Stacks → Create Stack** (`stackCreate`)
- Added **Stacks → Update Stack** (`stackUpdate`)
- Added **Stacks → Delete Stack** (`stackDelete`)
- Added **Stacks → Add Stack Config** (`stackConfigAdd`)
- Added **Stacks → Delete Stack Config** (`stackConfigDelete`)
- Added **Stacks → Enable Stack** (`stackEnable`)
- Added **Stacks → Disable Stack** (`stackDisable`)
- Added **Stacks → Lock Stack** (`stackLock`)
- Added **Stacks → Unlock Stack** (`stackUnlock`)
- Added **Stacks → Reslug Stack** (`stackReslug`)
- Added **Stacks → Set Current Commit** (`stackSetCurrentCommit`)
- Added **Stacks → Import State** (`stackManagedStateImport`)
- Added **Stacks → Rollback State** (`stackManagedStateRollback`)
- Added **Stacks → Star Stack** (`starSet`)
- Added **Stacks → Search Stacks** (`searchStacks`)
- Added **Stacks → Migrate Vendor** (`stackMigrateVendor`)
- Added **Stacks → Sync Commit** (`stackSyncCommit`)
- Added **Stacks → Get State Download URL** (`stateDownloadUrl`)
- Added **Stacks → Get State Upload URL** (`stateUploadUrl`)
- Added **Terraform Providers → List Providers** (`providers`)
- Added **Terraform Providers → Get Terraform Provider** (`terraformProvider`)
- Added **Terraform Providers → List Terraform Providers** (`terraformProviders`)
- Added **Terraform Providers → Create Terraform Provider** (`terraformProviderCreate`)
- Added **Terraform Providers → Update Terraform Provider** (`terraformProviderUpdate`)
- Added **Terraform Providers → Delete Terraform Provider** (`terraformProviderDelete`)
- Added **Terraform Providers → Set Provider Visibility** (`terraformProviderSetVisibility`)
- Added **Terraform Providers → Create Provider Version** (`terraformProviderVersionCreate`)
- Added **Terraform Providers → Update Provider Version** (`terraformProviderVersionUpdate`)
- Added **Terraform Providers → Delete Provider Version** (`terraformProviderVersionDelete`)
- Added **Terraform Providers → Publish Provider Version** (`terraformProviderVersionPublish`)
- Added **Terraform Providers → Revoke Provider Version** (`terraformProviderVersionRevoke`)
- Added **Terraform Providers → Register Platform** (`terraformProviderVersionRegisterPlatform`)
- Added **Terraform Providers → Register Platform V2** (`terraformProviderVersionRegisterPlatformV2`)
- Added **Terraform Providers → List Providers in Space** (`providersInSpace`)
- Added **Tofu Workspaces → Search Tofu Workspaces** (`searchTofuWorkspaces`)
- Added **Tofu Workspaces → Unlock All Tofu Workspaces** (`tofuWorkspaceUnlockAll`)
- Added **VCS Agent Pools → List VCS Agent Pools** (`vcsAgentPools`)
- Added **VCS Agent Pools → Get VCS Agent Pool** (`vcsAgentPool`)
- Added **VCS Agent Pools → Create VCS Agent Pool** (`vcsAgentPoolCreate`)
- Added **VCS Agent Pools → Update VCS Agent Pool** (`vcsAgentPoolUpdate`)
- Added **VCS Agent Pools → Delete VCS Agent Pool** (`vcsAgentPoolDelete`)
- Added **VCS Agent Pools → Reset VCS Agent Pool** (`vcsAgentPoolReset`)
- Added **VCS Integrations → Azure DevOps → Get Azure DevOps Integration** (`azureDevOpsRepoIntegration`)
- Added **VCS Integrations → Azure DevOps → Create Azure DevOps Integration** (`azureDevOpsRepoIntegrationCreate`)
- Added **VCS Integrations → Azure DevOps → Update Azure DevOps Integration** (`azureDevOpsRepoIntegrationUpdate`)
- Added **VCS Integrations → Azure DevOps → Delete Azure DevOps Integration** (`azureDevOpsRepoIntegrationDelete`)
- Added **VCS Integrations → Azure DevOps → Test Azure DevOps Integration** (`azureDevOpsRepoIntegrationTest`)
- Added **VCS Integrations → Azure DevOps → Get Webhooks Endpoint** (`azureDevOpsWebhooksEndpoint`)
- Added **VCS Integrations → Bitbucket Cloud → Get Bitbucket Cloud Integration** (`bitbucketCloudIntegration`)
- Added **VCS Integrations → Bitbucket Cloud → Create Bitbucket Cloud Integration** (`bitbucketCloudIntegrationCreate`)
- Added **VCS Integrations → Bitbucket Cloud → Update Bitbucket Cloud Integration** (`bitbucketCloudIntegrationUpdate`)
- Added **VCS Integrations → Bitbucket Cloud → Delete Bitbucket Cloud Integration** (`bitbucketCloudIntegrationDelete`)
- Added **VCS Integrations → Bitbucket Cloud → Test Bitbucket Cloud Integration** (`bitbucketCloudIntegrationTest`)
- Added **VCS Integrations → Bitbucket Cloud → Regenerate Webhook Secret** (`bitbucketCloudIntegrationRegenerateWebhookSecret`)
- Added **VCS Integrations → Bitbucket Cloud → Get Webhooks Endpoint** (`bitbucketCloudWebhooksEndpoint`)
- Added **VCS Integrations → Bitbucket Cloud → Create Bitbucket Cloud Integration V2** (`bitbucketCloudIntegrationCreateV2`)
- Added **VCS Integrations → Bitbucket Cloud → Update Bitbucket Cloud Integration V2** (`bitbucketCloudIntegrationUpdateV2`)
- Added **VCS Integrations → Bitbucket Datacenter → Get Bitbucket Datacenter Integration** (`bitbucketDatacenterIntegration`)
- Added **VCS Integrations → Bitbucket Datacenter → Create Bitbucket Datacenter Integration** (`bitbucketDatacenterIntegrationCreate`)
- Added **VCS Integrations → Bitbucket Datacenter → Update Bitbucket Datacenter Integration** (`bitbucketDatacenterIntegrationUpdate`)
- Added **VCS Integrations → Bitbucket Datacenter → Delete Bitbucket Datacenter Integration** (`bitbucketDatacenterIntegrationDelete`)
- Added **VCS Integrations → Bitbucket Datacenter → Test Bitbucket Datacenter Integration** (`bitbucketDatacenterIntegrationTest`)
- Added **VCS Integrations → Bitbucket Datacenter → Regenerate Webhook Secret** (`bitbucketDatacenterIntegrationRegenerateWebhookSecret`)
- Added **VCS Integrations → Bitbucket Datacenter → Get Webhooks Endpoint** (`bitbucketDatacenterWebhooksEndpoint`)
- Added **VCS Integrations → GitHub → Get GitHub Integration** (`githubIntegration`)
- Added **VCS Integrations → GitHub → Update GitHub Integration** (`githubIntegrationUpdate`)
- Added **VCS Integrations → GitHub Enterprise → Get GitHub Enterprise Integration** (`githubEnterpriseIntegration`)
- Added **VCS Integrations → GitHub Enterprise → Create GitHub Enterprise Integration** (`githubEnterpriseIntegrationCreate`)
- Added **VCS Integrations → GitHub Enterprise → Update GitHub Enterprise Integration** (`githubEnterpriseIntegrationUpdate`)
- Added **VCS Integrations → GitHub Enterprise → Delete GitHub Enterprise Integration** (`githubEnterpriseIntegrationDelete`)
- Added **VCS Integrations → GitHub Enterprise → Test GitHub Enterprise Integration** (`githubEnterpriseIntegrationTest`)
- Added **VCS Integrations → GitHub Enterprise → Regenerate Webhook Secret** (`githubEnterpriseIntegrationRegenerateWebhookSecret`)
- Added **VCS Integrations → GitHub Enterprise → Get Webhooks Endpoint** (`githubEnterpriseWebhooksEndpoint`)
- Added **VCS Integrations → GitLab → Get GitLab Integration** (`gitlabIntegration`)
- Added **VCS Integrations → GitLab → Create GitLab Integration** (`gitlabIntegrationCreate`)
- Added **VCS Integrations → GitLab → Update GitLab Integration** (`gitlabIntegrationUpdate`)
- Added **VCS Integrations → GitLab → Delete GitLab Integration** (`gitlabIntegrationDelete`)
- Added **VCS Integrations → GitLab → Test GitLab Integration** (`gitlabIntegrationTest`)
- Added **VCS Integrations → GitLab → Regenerate Webhook Secret** (`gitlabIntegrationRegenerateWebhookSecret`)
- Added **VCS Integrations → GitLab → Get Webhooks Endpoint** (`gitlabWebhooksEndpoint`)
- Added **VCS Integrations → List VCS Integrations** (`vcsIntegrations`)
- Added **VCS Integrations → Get VCS Integration** (`vcsIntegration`)
- Added **VCS Integrations → Search VCS Integrations** (`searchVCSIntegrations`)
- Added **VCS Integrations → List Repositories** (`repositories`)
- Added **VCS Integrations → List Branches** (`branches`)
- Added **VCS Integrations → Test VCS Integration** (`vcsIntegrationTest`)
- Added **Version Catalog → List Kubectl Versions** (`kubectlVersions`)
- Added **Version Catalog → List OpenTofu Versions** (`openTofuVersions`)
- Added **Version Catalog → Get OpenTofu Effective Version** (`openTofuEffectiveVersion`)
- Added **Version Catalog → List Terraform Versions** (`terraformVersions`)
- Added **Version Catalog → Get Terraform Effective Version** (`terraformEffectiveVersion`)
- Added **Version Catalog → List Terragrunt Versions** (`terragruntVersions`)
- Added **Version Catalog → Get Terragrunt Effective Version** (`terragruntEffectiveVersion`)
- Added **Webhooks → List Webhooks** (`searchNamedWebhooksIntegrations`)
- Added **Webhooks → Get Webhook** (`namedWebhooksIntegration`)
- Added **Webhooks → Create Webhook** (`namedWebhooksIntegrationCreate`)
- Added **Webhooks → Update Webhook** (`namedWebhooksIntegrationUpdate`)
- Added **Webhooks → Delete Webhook** (`namedWebhooksIntegrationDelete`)
- Added **Webhooks → Set Webhook Headers** (`namedWebhooksIntegrationSetHeaders`)
- Added **Webhooks → Delete Webhook Headers** (`namedWebhooksIntegrationDeleteHeaders`)
- Added **Worker Pools → List Worker Pools** (`workerPools`)
- Added **Worker Pools → Get Worker Pool** (`workerPool`)
- Added **Worker Pools → Create Worker Pool** (`workerPoolCreate`)
- Added **Worker Pools → Update Worker Pool** (`workerPoolUpdate`)
- Added **Worker Pools → Delete Worker Pool** (`workerPoolDelete`)
- Added **Worker Pools → Get Public Worker Pool** (`publicWorkerPool`)
- Added **Worker Pools → Search Worker Pools** (`searchWorkerPools`)
- Added **Worker Pools → Cycle Worker Pool** (`workerPoolCycle`)
- Added **Worker Pools → Reset Worker Pool** (`workerPoolReset`)
- Added **Worker Pools → Set Worker Drain** (`workerDrainSet`)
- Added **Runs → Delete Run State Summary** (`deleteRunStateSummary`)
- Added **Runs → Delete Run Summary** (`deleteRunSummary`)
- Added **Runs → Summarize Run** (`summarizeRun`)
- Added **Runs → Summarize Run State** (`summarizeRunState`)
